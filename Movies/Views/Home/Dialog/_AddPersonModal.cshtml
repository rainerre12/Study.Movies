@using Movies.Models.CustomModel
@model HomeViewModel


<div class="card">
    <div class="card-body">
        <h4 class="card-title text-center">Add Movies</h4>
        <div class="template-demo">

@*             @using (Html.BeginForm("RegisterPerson", "Home", FormMethod.Post, new { @class = "forms-sample" }))
            {
                <div class="form-group">
                    <label for="FirstName">First Name</label>

                    @Html.TextBoxFor(model => model.Persons.FirstName, new { @class = "form-control", id = "FirstName", placeholder = "First Name" })

                    <label for="LastName">Last Name</label>

                    @Html.TextBoxFor(model => model.Persons.LastName, new { @class = "form-control", id = "LastName", placeholder = "Last Name" })

                </div>

                <button id="addPersonBtn" class="btn btn-primary" type="submit">Submit</button>
                <button class="btn btn-light" data-dismiss="modal">Cancel</button>
            } *@

            <div class="form-group">
                <label for="FirstName">First Name</label>

                @Html.TextBoxFor(model => model.Persons.FirstName, new { @class = "form-control", id = "FirstName", placeholder = "First Name" })

                <label for="LastName">Last Name</label>

                @Html.TextBoxFor(model => model.Persons.LastName, new { @class = "form-control", id = "LastName", placeholder = "Last Name" })

            </div>

            <button id="addPersonBtn" class="btn btn-primary" type="button">Submit</button>
            <button class="btn btn-light" data-dismiss="modal">Cancel</button>

        </div>
    </div>
</div>
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="~/js/modal/AddPersonModal.js"></script>